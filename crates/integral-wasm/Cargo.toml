[package]
name = "integral-wasm"
description = "WASM bindings for the Integral web frontend"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
integral-core = { path = "../integral-core" }
wasm-bindgen = "0.2"
